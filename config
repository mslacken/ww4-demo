# networt configuration for the demo used for:
# - static congiguration of qcow2 ww4 image
# - terraform network
json_config=config.json
IPADDR=$(jq .IPADDR < ${json_config})
IPNET=$(jq .IPNET < ${json_config})
NETMASK=$(jq .NETMASK < ${json_config})
GATEWAY=$(jq .GATEWAY < ${json_config})
DNS=$(jq .DNS < ${json_config})
